[discord] # all the configs for the discord part of the bot
token = "" # Put your token here
prefix = ":3" # the prefix that every command begins with
admin_role = ""
mod_role = ""

[database]
name="owo_bot_db"
password="" # make sure this is the same as in config/db-passwd.txt (this is for docker)